services:
  - type: web
    name: ava-dating-site
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ava.wsgi
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_SETTINGS_MODULE
        value: ava.settings
